int countGoodSubstrings(char* s) {
    int n = strlen(s), cnt = 0;
    for (int i = 0; i < n - 2; ++i) {
        char a = s[i], b = s[i + 1], c = s[i + 2];
        if (a != b && b != c && c != a)
            cnt++;
    }
    return cnt;
}
